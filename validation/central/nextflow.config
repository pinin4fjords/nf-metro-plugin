plugins {
    id 'nf-metro@0.1.0'
}

process.executor = 'local'

executor {
    cpus      = 2
    queueSize = 4
}

metro {
    server = System.getenv('METRO_SERVER') ?: 'http://localhost:8080'
    map    = System.getenv('METRO_MAP')
}
