plugins {
    id 'nf-metro@0.1.0'
}

process.executor = 'local'

executor {
    cpus      = 2
    queueSize = 4
}

// Central mode: register this run's map on a persistent `serve-multi` server.
metro {
    server = 'http://localhost:8080'
    map    = "${projectDir}/../pipeline.mmd"
}
