plugins {
    id 'nf-metro@0.1.0'
}

process.executor = 'local'

executor {
    cpus      = 2
    queueSize = 4
}

// Attach mode: POST events to a server you started separately, e.g.
//   nf-metro serve validation/pipeline.mmd --port 8090
metro {
    url = 'http://localhost:8090/events'
}
